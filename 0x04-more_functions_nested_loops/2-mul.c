#include "main.h"
/**
  *mul - the multiplication function
  *@a:number
  *@b: number to be multioplieed
  *Return: return the result
  **/
int mul(int a, int b)
{
	int result = 0;

	result = a * b;
	return (result);
}
