#include "main.h"
/**
  *add- the function which add two numbers
  *@i: first number to be added
  *@j:numbers to be added
  *Return: returns the sum of two numbers i and j
  **/
int add(int i, int j)
{
int k;
k = i + j;
return (k);
}
