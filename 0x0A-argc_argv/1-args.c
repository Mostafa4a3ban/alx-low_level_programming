#include <stdio.h>
/**
 *main - the main function
 *@argc: the number of arguments
 *@argv: the arrays of strings
 *Return: Always 0.
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc - 1);
	return (0);
}
